<h1> Vehicles </h1>

<%= button_to "Add New Car", new_vehicle_path, method: :get %>
<ul>
    <% @vehicles.each do |vehicle| %>
        <li><%= link_to student.name, student_path(student)%></li>
    <%end%>
</ul>